/** A rendering is a single UI/transport value. Catalog length is not a draw limit. */
export const MAXIMUM_CARD_TEXT_BYTES = 8 * 1024;
export const MAXIMUM_CARD_TEXT_CHARACTERS = 4000;
export const MAXIMUM_CATALOG_VALUE_BYTES = 16 * 1024 * 1024;
